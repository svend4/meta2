[pytest]
python_files = bench_*.py
python_classes = Bench* Test*
python_functions = test_* bench_*
markers =
    benchmark: performance benchmarks (timing)
    memory: memory profiling benchmarks
addopts = -v --tb=short
